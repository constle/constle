apiVersion: constle.dev/v1alpha1
kind: AgentManifest
identity: { name: rich-demo, version: "1.2.0" }
sandbox:
  image: alpine:latest
  command: ["sh","-c","echo 'fetching data from api'; echo 'analysis complete: 3 findings'; echo 'writing report'"]
  network: { egress: restricted, allowed_hosts: ["api.groq.com","arxiv.org"] }
capabilities: [external_api]
spending: { max_per_run_usd: "0.10" }
